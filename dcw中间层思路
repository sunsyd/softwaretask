        ┌────────────┐
        │ 接收请求    │ ←─── 前端 POST JSON
        └────┬───────┘
             │
     ┌───────▼────────┐
     │ 解析 expression │
     │ 生成函数调用树  │  ←—— 例如将 sin(π/4)×√(16)... 解析成树结构
     └───────┬────────┘
             │
     ┌───────▼────────┐
     │ 调用后端接口    │ → POST 函数树给后端服务
     └───────┬────────┘
             │
     ┌───────▼────────┐
     │ 处理后端响应    │ ←─── 接收计算结果
     └───────┬────────┘
             │
        ┌────▼────┐
        │ 返回结果 │ → 返回前端
        └─────────┘
